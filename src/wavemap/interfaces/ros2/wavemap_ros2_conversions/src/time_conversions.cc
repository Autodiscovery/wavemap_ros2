#include "wavemap_ros2_conversions/time_conversions.h"
// Header-only — this .cc exists only so the library has something to compile
// for this translation unit and to keep the build system consistent.
